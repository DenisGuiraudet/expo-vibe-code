// Define the category type
export type Category = 'Appetizers' | 'Main Courses' | 'Desserts' | 'Beverages' | 'Specials';

// Define an order item type
export type OrderItem = {
  name: string;
  quantity: number;
};